[package]
name = "lisp-std-numbers"
version = "0.1.0"
authors = ["Nathan Faucett <nathanfaucett@gmail.com>"]

keywords = ["lisp", "std", "numbers"]
categories = []

description = "lisp std numbers"
license = "MIT/Apache-2.0"
readme = "README.md"
repository = "https://gitlab.com/nathanfaucett/rs-lisp.git"
homepage = "https://gitlab.com/nathanfaucett/rs-lisp"

[lib]
name = "lisp-std-numbers"
path = "src/lib.rs"
crate-type = ["dylib", "rlib"]

[dependencies]
lisp = { git = "https://gitlab.com/nathanfaucett/rs-lisp" }

[dev-dependencies]
